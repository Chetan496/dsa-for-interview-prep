# traverse a tree in preorder traversal
# root left right policy
class TreeNode:
    def __init__(self, val, left, right):
        self.val = val
        self.left = left
        self.right = right

    def left():
        